apiVersion: apps/v1
kind: Deployment
metadata:
  name: authservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: authservice
  template:
    metadata:
      labels:
        app: authservice
    spec:
      containers:
      - name: authservice
        image: ghcr.io/leocfig/authservice:latest
        ports:
        - containerPort: 8000
        env:
        - name: DB_HOST
          value: db-auth
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          value: auth_db
        - name: DB_USER
          value: auth_user
        - name: DB_PASSWORD
          value: auth_pass
        - name: JWT_SHARED_SECRET
          value: dev-shared-secret
        - name: DJANGO_SECRET_KEY
          value: dev-key
        - name: DJANGO_DEBUG
          value: "1"
        - name: CORS_ALLOWED_ORIGINS
          value: http://localhost:5173,http://127.0.0.1:5173
        - name: ALLOWED_HOSTS
          value: "*"
        resources:
          requests:
            cpu: "200m"
            memory: "300Mi"
          limits:
            cpu: "400m"
            memory: "300Mi"
        readinessProbe:
          httpGet:
            httpHeaders:
              - name: host
                value: localhost
            path: /health/
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
        livenessProbe:
          httpGet:
            httpHeaders:
              - name: host
                value: localhost
            path: /health/
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 30
      imagePullSecrets:
      - name: ghcr-secret
---
apiVersion: v1
kind: Service
metadata:
  name: authservice
  labels:
    app: authservice
spec:
  selector:
    app: authservice
  ports:
    - name: http-metrics
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: authservice-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: authservice
  minReplicas: 1
  maxReplicas: 3
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 50
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 60
