apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: authservice-monitor
  namespace: default
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: authservice
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
