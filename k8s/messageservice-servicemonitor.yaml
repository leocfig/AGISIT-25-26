apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: messageservice-monitor
  namespace: default
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: messageservice
  endpoints:
    - port: http-metrics
      path: /metrics
      interval: 15s
